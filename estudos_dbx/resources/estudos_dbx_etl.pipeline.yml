# The main pipeline for estudos_dbx

resources:
  pipelines:
    estudos_dbx_etl:
      name: estudos_dbx_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/estudos_dbx_etl"

      libraries:
        - glob:
            include: ../src/estudos_dbx_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
